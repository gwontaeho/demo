import { Button } from "../modules/button";

export const SampleButton = () => {
  return (
    <div>
      <Button role="submit"></Button>
      <Button role="cancel"></Button>
      <Button role="confirm"></Button>
    </div>
  );
};
